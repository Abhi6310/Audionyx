{{!-- <style>
    html, body {
    height: 100%;
    width: 100%;
    margin: 0;
    padding: 0;
    background-color: black !important; /* Entire page background color black */
    /*background-image: url(/Audionyx/AudionyxSource/src/resources/images/logo3.png) !important;
    color: white; /* Text color white for the entire page */
    font-family: Arial, sans-serif; /* Set the font */
}

/* Ensure the body takes the full height of the viewport */
/* body {
    display: flex;
    justify-content: center;
    align-items: center;
} */
.login-container {
    background: rgba(0, 0, 0, 0.75); /* Semi-transparent background for login container */
    padding: 20px;
    border-radius: 8px;
    width: 100%;
    max-width: 100%; /* Restrict container width to a maximum of 500px */
    text-align: center;
    border: 1px solid rgb(129, 26, 145); /* Add a white border for better visibility */
}
.login-button {
    background-color: rgb(129, 26, 145);
    color: white;
}
h2, h5 {
    color: white; /* Ensure headings are white */
}

.input-group, .form-group {
    margin-bottom: 15px;
    text-align: left;
}

label {
    color: white; /* Ensure labels are white */
}

input {
    width: 100%;
    padding: 10px;
    border: 1px solid white; /* Light border around input fields */
    border-radius: 4px;
    box-sizing: border-box;
    background-color: black; /* Input background color black */
    color: white; /* Input text color white */
}

button {
    width: 100%;
    padding: 10px;
    border: none;
    border-radius: 4px;
    background: #28a745; /* Button background color green */
    color: white; /* Button text color white */
    font-size: 16px;
    cursor: pointer;
}

button:hover {
    background: #218838; /* Darker green on hover */
}

/* Add specific styles for the navbar/header to ensure it's not affected */
.navbar, .header {
    width: auto !important; /* Override the width settings for navbar/header */
}

</style> --}}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <!-- Link to your CSS file -->
    <link rel="stylesheet" href="../resources/css/loginstyle.css">
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        {{> message}} <!-- if booked -->
        <div class="mt-5 login-container">
            <h2>Welcome back to Audionyx!</h2>
            <h5>Sign in to jump right back into your visual audio experience.</h5>
            <form action="/login" method="POST">
                <div class="form-group mb-3">
                    <label for="username">Username</label>
                    <input type="username" id="username" name="username" class="form-control" required>
                    <small class="form-text text-muted">We'll never share your information with anyone else.</small>
                </div>
                <div class="form-group mb-3">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" class="form-control" required>
                </div>
                <button type="submit" class="btn btn-primary">Login</button>
            </form>
            <div class="mt-3">
                <p>Don't have an account? <a href="/register">Sign up here</a>!</p>
            </div>
        </div>
    </div>

    <!-- jQuery Script -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <!-- Popper.js Script (Bootstrap 4 dependency) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
    <!-- Bootstrap Script -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <!-- Your Custom Script -->
    <script src="../resources/js/script.js"></script>
</body>